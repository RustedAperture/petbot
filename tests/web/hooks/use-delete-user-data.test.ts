import { describe, it, expect } from "vitest";

// placeholder test since hook cannot be called outside React environment

describe("useDeleteUserData (placeholder)", () => {
  it("sanity check", () => {
    expect(true).toBe(true);
  });
});

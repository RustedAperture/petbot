import { createElement } from "react";
// Minimal Next.js Image stub for use in Vitest tests (via test.alias in vite.config.ts)
const Image = (props: any) => createElement("img", props);
export default Image;
